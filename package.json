{
  "name": "sufi-ilham",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/verify-build.mjs",
    "build:only": "astro build",
    "verify": "node scripts/verify-build.mjs",
    "deploy": "npm run build && cp -r dist/. . && rm -f privacy.html terms.html contact.html dist/privacy.html dist/terms.html dist/contact.html && node scripts/verify-build.mjs --root",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.4",
    "@fontsource-variable/newsreader": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/instrument-serif": "^5.3.0",
    "astro": "^5.18.2",
    "sharp": "^0.35.4"
  }
}
